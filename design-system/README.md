# DPF-MVP Design System

This directory is now the human-readable overview layer.

Source-of-truth governance lives in workspace packages:

- `packages/design-tokens`
- `packages/icon-library`
- `packages/component-library`

Machine-readable orchestration lives in `design-system-engineering`.

Do not treat files in this directory as the canonical registry for tokens, components, icons, or package versions.
