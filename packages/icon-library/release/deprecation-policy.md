# Icon Library Deprecation Policy

Deprecated icon keys must include a replacement key, deprecation version, removal target, and runtime migration alias while compatibility is active.

Functional icons must remain local assets and must render through the governed Icon/AppIcon runtime entry.
