# Design Tokens Deprecation Policy

Deprecated tokens must include a replacement token, deprecation version, removal target, and migration note.

Compatibility paths under `design-system-engineering/01_tokens` and `src/theme` are temporary. They must not receive new token source-of-truth changes after package migration, except as compatibility mirrors.
