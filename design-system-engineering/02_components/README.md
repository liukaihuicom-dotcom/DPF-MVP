# Compatibility Mirror

Component-library source of truth has moved to `packages/component-library`.

Files in this directory remain as a temporary compatibility mirror for existing QA references and migration history. New base component governance changes must be made in `packages/component-library` first.

Business components remain governed separately under `design-system-engineering/03_business_components`.
