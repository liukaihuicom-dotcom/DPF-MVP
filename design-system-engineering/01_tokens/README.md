# Compatibility Mirror

Token source of truth has moved to `packages/design-tokens`.

Files in this directory remain as a temporary compatibility mirror for existing QA references and migration history. New token governance changes must be made in `packages/design-tokens` first, then mirrored here only while compatibility is active.
