# Compatibility Mirror

Icon-library source of truth has moved to `packages/icon-library`.

Files in this directory remain as a temporary compatibility mirror for existing QA references and migration history. New icon governance changes must be made in `packages/icon-library` first, then mirrored here only while compatibility is active.
