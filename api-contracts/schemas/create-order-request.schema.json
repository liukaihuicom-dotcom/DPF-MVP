{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "create-order-request.schema.json",
  "title": "CreateOrderRequest",
  "type": "object",
  "required": ["instrumentId", "direction", "type", "lots"],
  "properties": {
    "instrumentId": { "type": "string" },
    "direction": { "type": "string", "enum": ["buy", "sell"] },
    "type": { "type": "string", "enum": ["market", "limit", "stop"] },
    "lots": { "type": "number", "exclusiveMinimum": 0 },
    "expirationType": { "type": "string", "enum": ["gtc", "specified"] },
    "expiresAt": { "type": "string" },
    "limitPrice": { "type": "number" },
    "oneClickTradingEnabled": { "type": "boolean" },
    "stopLoss": { "type": "number" },
    "stopPrice": { "type": "number" },
    "takeProfit": { "type": "number" },
    "idempotencyKey": { "type": "string" }
  }
}
